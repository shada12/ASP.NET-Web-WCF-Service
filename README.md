# ASP.NET-Web-WCF-Service
 
•	Created and deployed ASP.NET Web (WCF) Service which manipulate XML file.
•	Created ASP.NET website as client of the Web (WCF) to get the information and consume Web (WCF).
•	Created PHP website as client of the Web (WCF) to get the information and consume Web (WCF).
